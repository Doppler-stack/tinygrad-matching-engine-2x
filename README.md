# tinygrad-matching-engine-2x
This repo contains my fork of tinygrad for the $1000 bounty to 2x the matching engine speed (400ms → 200ms) via changes in external_benchmark_schedule.py. See: https://bountyboard.dev/bounties and tinygrad PR guidelines.
